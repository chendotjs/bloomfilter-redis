environment:
  matrix:
    - nodejs_version: "4.7"
    - nodejs_version: "5.12"
    - nodejs_version: "6.9"
    - nodejs_version: "6.9"
cache:
  - node_modules
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install
build: off
test_script:
  - node --version
  - npm --version
  - npm run-script test
